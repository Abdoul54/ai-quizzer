/** Max file size per upload (5 MB) */
export const UPLOAD_MAX_FILE_SIZE_BYTES = 5 * 1024 * 1024;

/** Human-readable label used in error messages */
export const UPLOAD_MAX_FILE_SIZE_LABEL = "5 MB";

/** Max documents that can be attached to a single quiz */
export const QUIZ_MAX_DOCUMENTS = 3;